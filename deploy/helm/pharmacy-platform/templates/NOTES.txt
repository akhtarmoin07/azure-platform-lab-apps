The pharmacy platform chart has been rendered for namespace {{ .Release.Namespace }}.

Expected services:
  ui:       http://ui:{{ .Values.ui.service.port }}
  backend:  http://backend:{{ .Values.backend.service.port }}

The backend connects to {{ .Values.backend.database.name }} on
{{ .Values.backend.database.server }} by using AKS workload identity. Configure
the environment-specific managed identity client ID before deployment.
